# https://leetcode.com/problems/single-number-ii/description/


class Solution:
    # Other number appear three times, not two, so n^n=0 doesn't apply here
    # Doesn't apply to EOR
    # def singleNumber(self, nums: List[int]) -> int:
